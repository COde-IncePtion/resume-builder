language: node_js
node_js:
  - '10'
cache:
  directories:
    - node_modules
script:
  - 'npm test'
  - 'npm run build'
deploy:
  provider: pages
  skip_clean: true
  local_dir: build
  on:
    branch: master
